require('neogen').setup({
    enabled = true,
    -- languages = {
    --     go = {
    --         template = {
    --             annotation_convention = "godoc",
    --         },
    --     }
    -- }
})
